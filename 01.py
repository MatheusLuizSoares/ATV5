import requests,datetime,os, sys, json

servidores_campi.csv 
 strNomeArq = f"{strDiretorio}/cartola_fc_{ano}.json"
        with open(strNomeArq, "r", encoding='UTF-8') as dictOpen:
            dic_cartola = json.load(dictOpen)
            dictOpen.close()
            break